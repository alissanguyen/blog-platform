export enum SupportedTheme {
    DARK = "dark",
    LIGHT = "light"
  }

  export enum ContactFormFields {
    email = "email",
    name = "name",
    subject = "subject",
    message = "message"
  }

//   NOTE: Can modify this to add/remove type of alert
  export enum AlertType {
    ERROR = "error",
    SUCCESS = "success",
    CONFIRMED = "confirmed"
  }

  